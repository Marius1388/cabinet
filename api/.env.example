# Server-side secrets for the contact API.
# On cPanel, set these as environment variables in "Setup Node.js App"
# (or copy this file to .env in the app root).
EMAIL=
EMAIL_PASS=
RECAPTCHA_SECRET_KEY=
# PORT is only used for local development (cPanel/Passenger assigns its own)
PORT=3001
